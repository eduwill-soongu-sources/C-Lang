#include <stdio.h>

int main(void) {
    
    int le_sserafim_ages[] = {24, 26, 23, 21, 18};

    // le_sserafim_ages[0] = 24;
    // le_sserafim_ages[1] = 26;
    // le_sserafim_ages[2] = 23;
    // le_sserafim_ages[3] = 21;
    // le_sserafim_ages[4] = 18;

    printf("르세라핌 리더 채원이의 나이는? %d살\n", le_sserafim_ages[0]);
    printf("르세라핌 막내 은채의 나이는? %d살\n", le_sserafim_ages[4]);

    // printf("6번째 방에는 누가살아? %d\n", le_sserafim_ages[5]);

    return 0;
}